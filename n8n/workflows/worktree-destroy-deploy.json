{
  "name": "Worktree Destroy Deploy",
  "nodes": [
    {
      "parameters": {},
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [200, 300]
    },
    {
      "parameters": {
        "functionCode": "const slug = $json.slug || 'sample-worktree';\nconst escaped = (value) => `'${String(value).replace(/'/g, `'\"'\"'`)}'`;\nreturn [{ command: `cd /home/gandalfalex/projects/coding-agent-workflow && infra/worktree-deployer/scripts/destroy-worktree-deploy.sh --slug ${escaped(slug)}` }];"
      },
      "name": "Build Command",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [420, 300]
    },
    {
      "parameters": {
        "sshCommand": "={{$json.command}}"
      },
      "name": "SSH Destroy Deploy",
      "type": "n8n-nodes-base.ssh",
      "typeVersion": 1,
      "position": [680, 300],
      "credentials": {
        "ssh": {
          "name": "orangepi"
        }
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Build Command",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Command": {
      "main": [
        [
          {
            "node": "SSH Destroy Deploy",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
